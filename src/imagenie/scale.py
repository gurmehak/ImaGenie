
def scale(image, N):
    """Scale an image file by a factor of N.

    Parameters
    ----------
    image : np array
        An image file to be scaled.
    
    N: integer
        An integer specifying the scaling factor.

    Returns
    -------
    np array
        An image file that has been scaled.
        
    Raises:
    ------
    ValueError
        If the factor N is not an integer.

    Examples
    --------
    Scale the image 2 times larger. 
    >>> img = scale("testimage.jpg", 2)
    """
    
    pass